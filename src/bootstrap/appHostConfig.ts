export type AppKey = 'main' | 'template';

// Set this to 'main' or 'template' for a quick local app swap during development.
export const LOCAL_APP_OVERRIDE: AppKey | null = null;

export const TEMPLATE_APP_SEGMENT = '/template';
